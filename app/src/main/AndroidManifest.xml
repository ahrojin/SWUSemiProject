<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.swusemiproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TakeCamera"></activity>
        <activity android:name=".Camera" />
        <activity android:name=".WriteMemoActivity" />
        <activity android:name=".NewMemoActivity" />
        <activity android:name=".Fragment_1" />
        <activity android:name=".MemoRecyclerActivity" />
        <activity android:name=".FragUserInfoActivity" />
        <activity android:name=".FragMemoActivity" />
        <activity android:name=".TabActivity" />
        <activity android:name=".MemoActivity" />
        <activity android:name=".SignIn" />
        <activity android:name=".MemoTabActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>